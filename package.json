{
  "name": "encryption-busybox",
  "version": "1.0.0",
  "description": "A busybox that wraps the node-jose library with APIs to demostrate JSON Web Encryption.",
  "main": "main.js",
  "files" : [

  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "node-jose": "^0.11.0",
    "redis": "^2.8.0"
  },
  "pkg": {
    "assets": [
      "static/**/*"
    ],
    "targets": [
      "node9-linux-x64"
    ]
  }
}
